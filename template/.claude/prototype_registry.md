# Prototype Registry

> Provisional-surface totality record — the second half of the `// PROVISIONAL(<slug>)` contract. Rule: `.claude/skills/prototype/SKILL.md` → `## PROVISIONAL` and `## Promotion`. Enforced commit-time by `.claude/hooks/provisional_totality_guard.py` (`.cs` markers only; scene-only surfaces are author-discipline plus the promotion sweep).

<!-- PROJECT-OWNED SEED: one row per PROVISIONAL slug; an empty table is fine at project start. -->

## Entries

| Slug | Surface | Landed | Boundary | Condition | Status |
|---|---|---|---|---|---|
