# Worklog Titles
<!--
Auto-generated mirror of `DevProjects/{{PROJECT_NAME}}/Claude/TODO/Worklog.md` (Obsidian).
DO NOT HAND-EDIT — the `/worklog` command patches this file incrementally on every Active-section change (full regeneration happens only via `/worklog show`).

Purpose: lightweight always-loaded view (titles + class + scope) for the SessionStart hook.
Source of truth lives in Obsidian. This is a derived artifact, but committed
to the repo so a fresh checkout has worklog awareness without an MCP round-trip.

Format: `<category> · <class> · <scope> · <title>` (with optional ` [after: <condition>]` and/or ` [quick-win]` suffixes)
Active [ ] items only.
-->

Last synced: (never — run /worklog show after creating the Obsidian worklog doc)

## Active
