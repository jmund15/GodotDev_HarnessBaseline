# Model ladder — candidate claims from external sources (unverified)

Split out of `model_ladder_evidence.md` on 2026-09-14 so the ladder stays a pick surface. Nothing here is a routing verdict; each row names the measurement that would convert it into one. Owner of the verdicts: `reference/model_ladder_evidence.md`.

Opinion, not measurement. Behavioral claims from `Docs/Transcripts/fable-vs-astra-debate-is-over-P7bxbDSnZRM.md` (single-operator YouTube comparison of Fable 5.1 vs GPT-6 Astra, retrieved 2026-09-10). No rail changes until a matched measurement lands.

| ID | claim | source | target surface | verifying measurement |
|---|---|---|---|---|
| C1 | Fable output quality stays within ±2 points across prompts; Astra alternates jaw-dropping peaks with worst-of-year valleys. | transcript L682-691, L1891-1950 | `orchestration` §5 pin choice | boundary-cell reps across varied seeds reported as pass RATE, per `benchmark_design` §3; variance as its own axis. |
| C2 | Fable PRs merge after ~2 follow-ups vs ~6 for Astra (~20% more mergeable); both regressions in a 150-PR yolo sample came from Astra. | transcript L909-927, L1159-1164, L2014-2021 | T6/T9/T10 ladder row ("ceilinged") | T6/T9/T10 cells scored with follow-up-count and regression-count beside the pass bit (`benchmark_design` §4 LOW EVENT COUNT, §7 sub-axes separately). |
| C3 | Astra validates every assumption exhaustively and finds bugs outside what Fable grasps from reading alone, at ~4x time/tokens. | transcript L636-681 | astra ladder row; `guards/review.md` `## strict` | T8/T11 cells at astra·high vs opus·xhigh with token accounting; beyond-key yield (`benchmark_design` §11) credits finds outside the key. |
| C4 | A PR Astra cannot land becomes landable by handing it to Fable with a "make this land, throw away the rest" brief. | transcript L1073-1087 | `orchestration` §5 escalation | paired handoff cell: stuck-arm artifact → closer-arm land rate plus diff-size delta. |
| C5 | Astra scope-creeps by default (50-line fix becomes a 1000-line PR); review-agent reminders bloat its context off-course. | transcript L1094-1117 | `guards/author.md` `## strict` | T6/T9 execution cells measuring diff size against spec scope, with the §9 hole-in-prompt controls. |
| C6 | Astra ignored "revert" stated twice, changed the wrong target, and merged a PR without making the requested changes; the verbatim Fable prompt landed first try. | transcript L1190-1343 | `guards/author.md` `## strict`; T8 ladder row | T8 intent-adherence probe with keyed revert/redirect items; CONTRADICT probe (`benchmark_design` §5). |
| C7 | Fable pulls in a skill by description and applies it across turns; Astra drops a used skill after one turn and ignores it for five follow-ups. | transcript L1497-1531 | `hooks/session_model_rails.py` tier lines; `guards/any.md` `## strict` | multi-turn skill-adherence probe: same skill relevant on turns 1 and 3, score application on turn 3. |
| C8 | Astra absorbs mid-run injected context (steering, 40-agent updates) without losing the task; earlier models derail. | transcript L1407-1467 | `orchestration` §5 dispatch pattern | steering probe: inject a correction mid-run, score completion of the original plus the injected items. |
| C9 | Astra holds 40 parallel subagents with message passing and drove a TS→Rust port 30%→80%+ test accuracy unsupervised. | transcript L693-760, L1355-1396 | `hooks/session_model_rails.py` provider-rails COST SHAPE | fan-out width experiment on one task: width × completion with token accounting; qualifies the few-long-agents knob. |
| C10 | Astra used 27K tokens where Fable 5.1 used ~80K for equal score; $3.26 vs $7.60 cost per task at equal list price. | transcript L1626-1640, L1661-1670 | ladder cost axis | per-cell token accounting with cache reads/writes read APART, per the cost-axis definition. |
| C11 | Cache writes are >60% of Fable cost while cache reads are ~3%; list-price gaps mislead. | transcript L1556-1600 | ladder cost axis | same accounting as C10; confirms reading cost components apart. |
| C12 | Running Astra always on its fastest/lowest rung misses its strengths and burns tokens for fun. | transcript L1771-1782 | ladder effort column | effort-bracket closure per `benchmark_design` §3: a pin is cited only once a pass sits beside a fail. |
| C13 | Agent-written skills compound into a slop loop; hand-write or audit every skill. | transcript L1479-1496 | `commands/codify.md` retirement loop (B6) | beyond-key yield review (`benchmark_design` §11): audit agent-authored artifacts before keying them. |
| C14 | Astra over-applies learned patterns: 21 unnecessary ALL-CAPS subtitles on one page. | transcript L373-407 | ladder taste axis; prose-delegation choice | prose task with a pattern-count metric beside the quality score (`benchmark_design` §7 sub-axes). |
| C15 | Astra forgets stated boundaries and its failures are more egregious; it also refuses security-sensitive asks more often than Fable. | transcript L1532-1552, L2028-2033 | `guards/any.md` `## strict` | boundary-adherence probe plus refusal-rate count as separate sub-axes (`benchmark_design` §7). |

Standing against existing ladder evidence, with the row cited:

- Consistent: C3 with the astra row (review/debugging tops); C10–C11 with the cost-axis definition (components read apart); C12 with the fable effort cell (low never on judgment turns); C7 with the deleg gap (fable 9 vs astra —).
- Tension: C2 against the T6/T9/T10 "ceilinged" row (ceiling on the pass bit, not on follow-ups); C6 against the T8 standing where astra cleared the tie alone (intent-heavy fix shape needs its own sub-axis); C9 against the provider-rails few-long-agents knob.
- No standing: C1, C4, C5, C8, C13, C14 unmeasured; C15 partly untestable while astra stays excluded.
